name: Quality checks
on:
  pull_request:
    paths: ["app/**", "jobradar/src/**", "jobradar/tests/**", "jobradar/package*.json", "shared/**", ".github/workflows/quality.yml"]
  push:
    branches: [main, "feat/**"]
    paths: ["app/**", "jobradar/src/**", "jobradar/tests/**", "jobradar/package*.json", "shared/**", ".github/workflows/quality.yml"]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory: [app, jobradar]
    defaults:
      run:
        working-directory: ${{ matrix.directory }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - if: matrix.directory == 'app'
        run: npm run build
